version: "3.9"
services:
  frontend:
    build: .
    ports:
      - "8080:80"
    depends_on:
      - backend

  backend:
    build: ../SVL_Server
    ports:
      - "7777:7777"
    env_file:
      - .env
